Previous File: OUTPUT/commons-io/prevFiles/prev_efe745src#java#org#apache#commons#io#FileSystemUtils.java
Revised File: OUTPUT/commons-io/revFiles/efe745src#java#org#apache#commons#io#FileSystemUtils.java
DiffEntry: @@ -224,3 +224,3 @@
         // read in the output of the command to an ArrayList
-        List lines = performCommand(cmdAttribs, Integer.MAX_VALUE);
+        List<String> lines = performCommand(cmdAttribs, Integer.MAX_VALUE);
         

@@ -231,3 +231,3 @@
         for (int i = lines.size() - 1; i >= 0; i--) {
-            String line = (String) lines.get(i);
+            String line = lines.get(i);
             if (line.length() > 0) {

