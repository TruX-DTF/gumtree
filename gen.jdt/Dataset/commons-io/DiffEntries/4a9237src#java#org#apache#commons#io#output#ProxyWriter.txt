Previous File: OUTPUT/commons-io/prevFiles/prev_4a9237src#java#org#apache#commons#io#output#ProxyWriter.java
Revised File: OUTPUT/commons-io/revFiles/4a9237src#java#org#apache#commons#io#output#ProxyWriter.java
DiffEntry: @@ -1,3 +1,3 @@
 /*
- * Copyright 2002-2004 The Apache Software Foundation.
+ * Copyright 2002-2005 The Apache Software Foundation.
  * 

@@ -22,3 +22,2 @@
 /**
- * 
  * A Proxy stream which acts as expected, that is it passes the method 

@@ -29,2 +28,6 @@
  * and write(String) to write(String, int, int).
+ * 
+ * @author Henri Yandell
+ * @author Stephen Colebourne
+ * @version $Id$
  */

@@ -32,7 +35,6 @@
 
-    private Writer proxy;
-
     /**
      * Constructs a new ProxyWriter.
-     * @param proxy Writer to delegate to
+     * 
+     * @param proxy  the Writer to delegate to
      */

@@ -40,3 +42,3 @@
         super(proxy);
-        this.proxy = proxy;
+        // the proxy is stored in a protected superclass variable named 'out'
     }

@@ -45,3 +47,3 @@
     public void write(int idx) throws IOException {
-        this.proxy.write(idx);
+        out.write(idx);
     }

@@ -50,3 +52,3 @@
     public void write(char[] chr) throws IOException {
-        this.proxy.write(chr);
+        out.write(chr);
     }

@@ -55,3 +57,3 @@
     public void write(char[] chr, int st, int end) throws IOException {
-        this.proxy.write(chr, st, end);
+        out.write(chr, st, end);
     }

@@ -60,3 +62,3 @@
     public void write(String str) throws IOException {
-        this.proxy.write(str);
+        out.write(str);
     }

@@ -65,3 +67,3 @@
     public void write(String str, int st, int end) throws IOException {
-        this.proxy.write(str, st, end);
+        out.write(str, st, end);
     }

@@ -70,3 +72,3 @@
     public void flush() throws IOException {
-        this.proxy.flush();
+        out.flush();
     }

@@ -75,3 +77,3 @@
     public void close() throws IOException {
-        this.proxy.close();
+        out.close();
     }

